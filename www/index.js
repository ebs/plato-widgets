require([ 'main' ], function(common) {
	require([ "jquery", "underscore", "backbone", "application" ], function($, _, Backbone, platon) {
		// Start Dashboard Application Here
		$("#content").html("Started");
	});
});